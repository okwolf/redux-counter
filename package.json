{
  "name": "redux-counter",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "cra-hyperapp": "0.1.0",
    "hyperapp": "1.2.6"
  },
  "scripts": {
    "format": "npx prettier --write src/**/*.{css,js}",
    "start": "hyperapp-scripts start",
    "build": "hyperapp-scripts build"
  }
}
